#include "main.h"

/**
 * _abs - calculate the absolute value of a number
 * @n: a number
 * Return: absolute value of a number
 */

int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}

	return (n);
}
