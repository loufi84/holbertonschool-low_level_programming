#include <stdio.h>

/**
 * main - Entry point
 * @argc: number of arguments
 * @argv: value of arguments
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);

	(void)argv;

	return (0);
}
